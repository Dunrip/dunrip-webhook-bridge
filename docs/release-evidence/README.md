# Release Evidence Artifacts

Generated by:

```bash
BASE_URL=https://<prod-url> RELEASE_VERSION=vX.Y.Z make post-release-verify
```

Each run creates a timestamped folder containing health checks, workflow status snapshot, benchmark comparison (when feasible), and a summary.
